node_modules/
*.log
.DS_Store

# client 构建产物（按需 commit；不在 npm 的 files 白名单内）
lib/client.js
lib/client.tmp.js

# 工具 / 编辑器残留（绝不进 git）
.dsh-vision-router/
.reasonix/

# 本地资产备份（webp 压缩前的原图，仅本地参考用）
assets/**/.archive/

# 同步脚本临时文件
*.bak
*.tmp

# 测试产物（未来用）
coverage/
dist/
